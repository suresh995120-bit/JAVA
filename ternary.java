public class ternary {
    public static void main(String[] args) {
        int a = 5;
        int b = 3;
        // int c = (a > b) ? a : b;
        // int c = (a < b) ? a : b;
        // int c = (a >= b) ? a : b;
        // int c = (a <= b) ? a : b;
        // int c = (a == b) ? a : b;
        // int c = (a != b) ? a : b;

        System.out.println((a != b) ? a : b);
    }
}
